## Summary

## Component(s)

- [ ] contracts
- [ ] backend
- [ ] frontend
- [ ] scripts
- [ ] docs

## Checklist

- [ ] `aiken fmt --check && aiken check && aiken build` passes (if `contracts/` changed)
- [ ] `npm run lint && npm run build && npm test` passes for each changed subproject
- [ ] Docs updated if behavior or setup steps changed
- [ ] If the validator changed: re-deployed to Preprod and updated the README's live deployment section
